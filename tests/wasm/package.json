{
  "name": "toonify-wasm-tests",
  "version": "1.0.0",
  "description": "Playwright browser tests for TOONify WASM bindings",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "server": "node server.js",
    "install-browsers": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}

